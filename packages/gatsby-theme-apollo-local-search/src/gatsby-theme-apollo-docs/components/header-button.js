/**
 * The original search component is turned on and off
 * by enabling an external search api.  Here we hijack
 * the unused header-button to search instead.
 */
export { Search as default } from './search';
